## Note-CMD ##

### install ###
`$ npm install`

### addNote ###
`$ node app add -title "..." -body "..."`

### removeNote ###
`$ node app remove -title "..."`

### readNote ###
`$ note app read -title "..."`

### listNote ###
`$ note app list`

### help ###
`$ node app help`
